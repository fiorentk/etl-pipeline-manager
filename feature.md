### User
- user has 2 role, admin and user
- admin can add new user and do everything
- user can CRUD records (datasource, dataflow, destination)

### Records:
- Datasource, Dataflow, Destination can be edited and deleted by all users
- Datasource, Dataflow, Destination can be created in separated menu
- Datasource, Dataflow, Destination can be linked in 1 record (ETL table). Like i can connect datasource A, with Dataflow B, and Destination C in 1 record. so  Datasource, Dataflow, Destination has their own table, and then we can create new ETL record by selecting datasource, dataflow, and destination. that is the main data that we wanna show in this website.

### main thing:
intinya website bisa create 3 record (datasource, dataflow, destination) and they can create new record that basically only selecting them and link them in 1 record (ETL table). 
